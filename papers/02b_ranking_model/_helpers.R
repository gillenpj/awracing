# _helpers.R
# Plotting / evaluation helpers used inside the paper-2b .qmd files.
# Kept inside papers/02b_ranking_model/ so the paper renders as a
# self-contained project. Not sourced by the pipeline.
#
# (Empty stub for now — add helpers here as the section files need them.)
